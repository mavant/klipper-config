[gcode_macro PROBE_ACTIVATE_HOOK]
description: Run before
gcode:
  RESPOND MSG="Activating probe"

[gcode_macro PROBE_DEACTIVATE_HOOK]
description: Run before
gcode:
  RESPOND MSG="Deactivating probe"
